<%- include("header") %>
    <h2>Empreses</h2>
    <% msgs.forEach((msg,empresa)=>{ %>
        <div class="card" style="width: 18rem;">
            <ul class="list-group list-group-flush">
                <li class="list-group-item">
                    <div class="info">
                        <p><b><%= msg.nom %></b></p>
                        <p><%= msg.email %></p>
                    </div>
                </li>
            </ul>
        </div>
    <% }) %>
    <button onclick="location.href='/empresa'" class="btn btn-primary">Crear empresa</button>
<%- include("footer") %>